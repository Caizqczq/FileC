@model ErrorViewModel
@{
    ViewData["Title"] = "错误";
}

<div class="container">
    <h1 class="text-danger">错误。</h1>
    <h2 class="text-danger">处理您的请求时发生错误。</h2>

    @if (Model.ShowRequestId)
    {
        <p>
            <strong>请求 ID：</strong> <code>@Model.RequestId</code>
        </p>
    }

    <div class="card mt-4">
        <div class="card-body">
            <h3>开发模式</h3>
            <p>
                切换到<strong>开发</strong>环境将显示有关发生的错误的更详细信息。
            </p>
            <p>
                <strong>部署的应用程序不应启用开发环境。</strong>
                它可能会向最终用户显示异常中的敏感信息。
                对于本地调试，请通过将<strong>ASPNETCORE_ENVIRONMENT</strong>环境变量设置为<strong>Development</strong>
                并重新启动应用程序来启用<strong>开发</strong>环境。
            </p>
        </div>
    </div>
</div>
